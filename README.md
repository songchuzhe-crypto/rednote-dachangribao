# bigtech-daily

Minimal Python package skeleton using a src layout.

## Run

```bash
python -m bigtech_daily run
```

## Render static reports

Generate `out/YYYY-MM-DD/daily.json`, then render the static HTML pages:

```bash
scripts/run_daily.sh
```
